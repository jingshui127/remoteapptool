# RemoteApp 创建客户端连接功能使用指南

## 📋 功能概述

**创建客户端连接**功能允许您为已配置的 RemoteApp 生成客户端连接文件（RDP 或 MSI 安装包），以便在客户端计算机上快速部署和使用 RemoteApp。

---

## 🎯 支持的连接类型

### 1. RDP 文件（推荐）
- ✅ 无需额外工具
- ✅ 文件小巧，易于分发
- ✅ 支持 RDP 文件签名
- ✅ 可选择是否创建未签名备份
- ℹ️ 用户需要双击 RDP 文件启动 RemoteApp

### 2. MSI 安装包（高级）
- ✅ 自动创建桌面快捷方式
- ✅ 自动创建开始菜单快捷方式
- ✅ 支持文件类型关联
- ✅ 支持卸载功能
- ⚠️ **需要安装 WiX Toolset 3.x**
- ℹ️ 用户安装后可直接从快捷方式启动

---

## 🚀 使用步骤

### 步骤 1：选择 RemoteApp

1. 在主窗口中选择一个已创建的 RemoteApp
2. 点击 **"创建客户端连接"** 按钮（或使用工具栏图标）

### 步骤 2：配置连接选项

#### 基本设置
- **服务器地址**：RemoteApp 服务器的地址（默认为本机名称）
- **备用服务器地址**：当主服务器不可用时使用的备用地址
- **服务器端口**：RDP 连接端口（默认 3389）

#### 远程桌面网关（可选）
- ☑️ **使用 RD 网关**：启用远程桌面网关
- **网关地址**：RD 网关服务器地址
- ☑️ **尝试直接连接**：优先尝试直接连接，失败后使用网关

### 步骤 3：选择连接类型

#### 选项 A：RDP 文件
1. 选择 **"RDP 文件"** 单选按钮
2. 配置 RDP 文件选项：
   - ☑️ **创建 Web 图标**：生成 .ico 图标文件
   - ☑️ **保存后用记事本打开**：方便查看和编辑 RDP 内容

#### 选项 B：MSI 安装程序
1. 选择 **"MSI 安装程序"** 单选按钮
   - ⚠️ 如果未安装 WiX Toolset，此选项将被禁用
2. 配置 MSI 选项（进入"MSI 选项"标签页）：

##### 快捷方式设置
- ☑️ **桌面快捷方式**：在桌面创建快捷方式
- ☑️ **开始菜单快捷方式**：在开始菜单创建快捷方式
  - ⚪ **子文件夹**：在开始菜单中创建应用程序子文件夹
  - ⚪ **顶级项目**：直接放在开始菜单根目录

##### 快捷方式标签
- ☑️ **使用快捷方式标签**：为快捷方式添加自定义标签
- **标签文本**：自定义标签（默认为 "remote"）
  - 例如：快捷方式名称会变成 "应用名称 (remote)"

##### 安装范围
- ⚪ **所有用户（计算机级别）**：需要管理员权限
- ⚪ **当前用户**：仅为当前用户安装

##### 文件类型关联
- ☑️ **禁用文件类型关联**：不在 MSI 中包含文件类型关联
- **FTA 计数**：显示关联的文件类型数量
- **编辑 FTA**：修改文件类型关联（仅影响此客户端连接）

### 步骤 4：RDP 签名（可选）

如果您有代码签名证书，可以为 RDP 文件签名以增强安全性：

1. 切换到 **"签名"** 标签页
2. ☑️ **签名 RDP**：启用 RDP 文件签名
3. **选择证书**：从下拉列表选择已安装的证书
4. ☑️ **创建已签名和未签名的副本**：同时保存两个版本

⚠️ **注意**：
- 需要 `rdpsign.exe` 工具（通常在 Windows SDK 中）
- 需要有效的代码签名证书
- 如果工具或证书缺失，签名选项将被禁用

### 步骤 5：保存连接文件

1. 配置完成后，点击 **"确定"** 或 **"保存"** 按钮
2. 选择保存位置和文件名
3. 等待文件生成完成

---

## 📝 高级功能

### RDP 选项

点击 **"RDP 选项"** 按钮可以配置额外的 RDP 连接参数：
- 显示设置
- 音频重定向
- 打印机重定向
- 剪贴板重定向
- 设备重定向
- 等等...

### 保存设置

- **保存设置**：将当前配置保存为默认设置
- **重置设置**：恢复为默认配置

保存的设置包括：
- 连接类型（RDP/MSI）
- 服务器地址和端口
- MSI 快捷方式选项
- RDP 签名选项
- 等等...

---

## 🎯 使用场景

### 场景 1：快速分发（推荐 RDP）
**适用于**：需要快速部署，用户熟悉 RDP 文件

1. 选择 RemoteApp
2. 点击"创建客户端连接"
3. 选择 RDP 文件
4. 配置服务器地址
5. 保存 RDP 文件
6. 将 RDP 文件发送给用户

**优点**：
- 文件小巧（几 KB）
- 无需安装
- 易于更新

### 场景 2：企业部署（推荐 MSI）
**适用于**：大规模部署，需要集成到企业桌面

1. 确保安装了 WiX Toolset
2. 选择 RemoteApp
3. 点击"创建客户端连接"
4. 选择 MSI 安装程序
5. 配置快捷方式和安装选项
6. 保存 MSI 文件
7. 使用 GPO 或 SCCM 部署 MSI

**优点**：
- 自动创建快捷方式
- 支持文件类型关联
- 用户体验更好
- 可通过企业部署工具推送

### 场景 3：安全部署（RDP + 签名）
**适用于**：安全要求高的环境

1. 确保有代码签名证书
2. 确保安装了 rdpsign.exe
3. 选择 RemoteApp
4. 点击"创建客户端连接"
5. 选择 RDP 文件
6. 启用"签名 RDP"
7. 选择证书
8. 保存签名的 RDP 文件

**优点**：
- 验证 RDP 文件完整性
- 防止篡改
- 增强用户信任

---

## ⚠️ 常见问题

### Q1: "MSI 安装程序" 选项被禁用
**A:** 需要安装 WiX Toolset 3.x

**解决方法**：
1. 访问 https://wixtoolset.org/
2. 下载并安装 WiX Toolset 3.x
3. 重启 RemoteApp Tool

### Q2: "签名" 标签页被禁用
**A:** 缺少 rdpsign.exe 或证书

**解决方法**：
1. 安装 Windows SDK（包含 rdpsign.exe）
2. 安装代码签名证书
3. 确保证书在"当前用户"或"本地计算机"存储中

### Q3: MSI 创建失败
**A:** 检查以下几点：
- WiX Toolset 是否正确安装
- 图标文件是否存在
- 是否有写入权限
- RDP 文件是否有效

**调试方法**：
1. 查看错误消息
2. 检查临时文件夹中的 .wxs 和 .wixobj 文件
3. 手动运行 candle.exe 和 light.exe 测试

### Q4: 文件类型关联不工作
**A:** MSI 中的文件类型关联仅在安装时创建

**注意**：
- 仅适用于 MSI 安装包
- RDP 文件不支持文件类型关联
- 如果已有其他程序关联了该文件类型，可能会冲突

### Q5: 服务器地址应该填什么？
**A:** 取决于您的网络环境：

- **局域网**：服务器计算机名或 IP 地址
- **互联网**：公网 IP 或域名
- **使用 RD 网关**：内网服务器地址 + 网关地址

### Q6: RDP 文件签名有什么用？
**A:** 增强安全性：
- 验证文件来源
- 防止篡改
- 减少安全警告
- 满足合规要求

---

## 💡 最佳实践

### 1. 命名规范
建议使用清晰的文件命名：
- RDP 文件：`AppName_ServerName.rdp`
- MSI 文件：`AppName_Setup.msi`

### 2. 版本管理
为不同版本的连接文件添加版本号：
- `AppName_v1.0.rdp`
- `AppName_Setup_v1.0.msi`

### 3. 文档说明
为用户提供简单的使用说明：
- 如何双击 RDP 文件
- 如何安装 MSI 包
- 如何处理安全警告

### 4. 测试部署
在正式分发前：
1. 在测试机器上验证 RDP/MSI
2. 测试所有功能
3. 检查快捷方式
4. 验证文件类型关联

### 5. 保存设置
使用"保存设置"功能：
- 统一配置标准
- 减少重复配置
- 提高效率

---

## 🔧 技术细节

### RDP 文件格式
生成的 RDP 文件包含：
```
full address:s:server.example.com
server port:i:3389
remoteapplicationname:s:应用程序名称
remoteapplicationprogram:s:||AppShortName
remoteapplicationmode:i:1
alternate shell:s:rdpinit.exe
```

### MSI 安装流程
1. 创建 RDP 文件
2. 提取图标文件（.ico）
3. 提取文件类型图标
4. 生成 WiX 源文件（.wxs）
5. 编译为 WiX 对象文件（.wixobj）
6. 链接为 MSI 安装包（.msi）

### 文件位置
默认生成位置：
- **RDP 文件**：我的文档\RemoteApp
- **MSI 文件**：我的文档\RemoteApp
- **临时文件**：系统临时文件夹

---

## 📚 相关资源

- [WiX Toolset 官网](https://wixtoolset.org/)
- [Windows SDK 下载](https://developer.microsoft.com/windows/downloads/windows-sdk/)
- [RDP 文件格式参考](https://docs.microsoft.com/en-us/windows-server/remote/remote-desktop-services/clients/rdp-files)

---

**功能已完全实现！** 🎉

如有任何问题，请查看程序的调试输出或联系技术支持。
