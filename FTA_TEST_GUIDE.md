# 文件类型关联（FTA）功能测试指南

## 📋 测试前准备

### 系统要求
- **管理员权限**：文件类型关联需要修改注册表，必须以管理员身份运行程序
- **测试环境**：建议在虚拟机或测试环境中进行，避免影响生产系统
- **备份注册表**：建议先备份以下注册表路径
  - `HKEY_CLASSES_ROOT\.txt` （示例文件扩展名）
  - `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Terminal Server\TSAppAllowList\Applications`

---

## 🎯 测试功能概述

RemoteApp Tool的文件类型关联功能允许您：
1. 为RemoteApp关联一个或多个文件类型（如.txt、.pdf、.doc等）
2. 在本地计算机上创建文件类型关联
3. 管理和删除文件类型关联
4. 清理未使用的文件类型关联

---

## 📝 测试步骤

### 第一步：启动程序

1. 以**管理员身份**运行 RemoteApp Tool
   ```
   右键点击 "RemoteApp Tool.exe" → 选择 "以管理员身份运行"
   ```

2. 确认程序正常启动，显示主界面

---

### 第二步：创建测试RemoteApp

1. 点击主界面的 **"+"按钮**（创建RemoteApp）

2. 选择一个应用程序，例如：
   - **记事本**：`C:\Windows\System32\notepad.exe`
   - **画图**：`C:\Windows\System32\mspaint.exe`
   - **写字板**：`C:\Program Files\Windows NT\Accessories\wordpad.exe`

3. 填写基本信息：
   - **短名称**：`TestNotepad`
   - **完整名称**：`测试记事本`
   - **路径**：`C:\Windows\System32\notepad.exe`

4. 点击 **"保存"** 或 **"确定"**

---

### 第三步：配置文件类型关联

1. 在主界面选中刚创建的RemoteApp（TestNotepad）

2. 点击 **"编辑"按钮**（或双击RemoteApp）

3. 在编辑窗口中，找到并点击 **"文件类型关联"按钮**（FTA按钮）
   - ℹ️ **注意**：只有编辑现有RemoteApp时此按钮才启用
   - ℹ️ 新建RemoteApp时此按钮被禁用（需要先保存）

4. 在文件类型关联窗口中，点击 **"创建"按钮**

5. 配置文件类型关联：
   - **文件类型**：输入文件扩展名，如 `.txt`
   - **图标路径**：选择图标文件（默认使用RemoteApp的图标）
   - **图标索引**：选择图标索引（默认为0）

6. 点击 **"确定"** 保存关联

---

### 第四步：在本地计算机上设置关联

1. 在文件类型关联列表中，选中刚创建的关联（如 `.txt`）

2. 点击 **"设置关联"按钮**

3. 根据提示选择：
   - **如果该文件类型未被关联**：程序会创建新的关联
   - **如果已有关联（其他应用）**：会提示是否替换（⚠️ 谨慎操作）
   - **如果已有关联（RemoteApp Tool）**：会提示是否删除

4. 确认操作后，系统会：
   - 修改注册表 `HKEY_CLASSES_ROOT\.txt`
   - 创建文件类型处理程序
   - 设置默认图标

---

### 第五步：测试文件类型关联

1. 创建一个测试文件，例如：
   - 在桌面右键 → 新建 → 文本文档
   - 重命名为 `test.txt`

2. 查看文件图标：
   - ✅ **成功**：文件图标应该显示为您设置的RemoteApp图标
   - ❌ **失败**：仍然显示默认记事本图标

3. 双击文件：
   - ✅ **成功**：应该使用RemoteApp（notepad.exe）打开
   - ❌ **失败**：检查注册表设置是否正确

4. 右键点击文件，查看"打开方式"：
   - 应该看到RemoteApp作为默认程序

---

### 第六步：管理文件类型关联

#### 6.1 编辑关联

1. 在文件类型关联窗口，选中一个关联
2. 点击 **"编辑"按钮**
3. 修改图标路径或索引
4. 点击 **"确定"** 保存

#### 6.2 删除关联

1. 在文件类型关联窗口，选中一个关联
2. 点击 **"删除"按钮**
3. 确认删除
4. 关联会从RemoteApp配置中移除

#### 6.3 清理未使用的文件类型关联

1. 在主菜单中选择 **"工具"** → **"删除未使用的文件类型关联"**
2. 程序会扫描所有文件类型关联
3. 删除那些关联到不存在的RemoteApp的关联
4. 显示清理数量

---

## ✅ 预期结果

### 正常情况
- ✅ 文件类型关联成功创建
- ✅ 文件图标正确显示
- ✅ 双击文件使用正确的应用程序打开
- ✅ 可以编辑和删除关联
- ✅ 清理功能正常工作

### 异常情况处理
- ⚠️ **权限不足**：提示需要管理员权限
- ⚠️ **文件类型已被占用**：提示是否替换
- ⚠️ **RemoteApp不存在**：清理功能会自动删除

---

## 🔍 调试和验证

### 查看注册表设置

1. 打开注册表编辑器（regedit）

2. 检查文件扩展名关联：
   ```
   HKEY_CLASSES_ROOT\.txt
   ```
   - 默认值应该是 `txt_file`

3. 检查文件类型处理程序：
   ```
   HKEY_CLASSES_ROOT\txt_file
   ```
   - 应该有 `RemoteApp` 值，指向RemoteApp短名称
   - 应该有 `shell\open\command` 键，指向应用程序路径
   - 应该有 `DefaultIcon` 键，指向图标路径

4. 检查RemoteApp配置：
   ```
   HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Terminal Server\TSAppAllowList\Applications\TestNotepad
   ```
   - 应该有文件类型关联信息

---

## 🐛 常见问题

### Q1: 文件图标没有改变
**A:** 尝试以下操作：
1. 刷新桌面（F5）
2. 重启资源管理器
3. 清除图标缓存：
   ```cmd
   del /a /q "%localappdata%\IconCache.db"
   ```
4. 重新登录Windows

### Q2: 双击文件不使用RemoteApp打开
**A:** 检查：
1. 注册表中的命令路径是否正确
2. RemoteApp路径是否存在
3. 是否有其他程序覆盖了关联

### Q3: 提示权限不足
**A:** 
1. 确保以管理员身份运行程序
2. 检查UAC设置
3. 检查用户账户权限

### Q4: 清理功能无响应
**A:** 
1. 等待扫描完成（可能需要一些时间）
2. 检查是否有大量的文件类型关联
3. 查看调试输出

---

## 📊 测试检查清单

- [ ] 程序以管理员权限启动
- [ ] 成功创建测试RemoteApp
- [ ] FTA按钮在编辑窗口中可见且可用
- [ ] 成功创建文件类型关联
- [ ] 文件图标正确显示
- [ ] 双击文件使用正确的程序打开
- [ ] 可以编辑关联
- [ ] 可以删除关联
- [ ] 清理未使用的FTA功能正常
- [ ] 注册表项正确创建
- [ ] 测试后成功清理

---

## 🧹 测试后清理

### 删除测试关联
1. 在RemoteApp编辑窗口中删除文件类型关联
2. 或使用"清理未使用的FTA"功能

### 删除测试RemoteApp
1. 在主界面选中TestNotepad
2. 点击删除按钮

### 恢复系统关联
1. 如果替换了系统默认关联，可以：
   - 右键点击文件 → 打开方式 → 选择其他应用
   - 选择正确的程序并勾选"始终使用此应用打开"

---

## 💡 高级测试场景

### 测试多个文件类型
为同一个RemoteApp关联多个文件类型：
- `.txt` - 文本文件
- `.log` - 日志文件
- `.ini` - 配置文件

### 测试不同RemoteApp的同一文件类型
测试冲突处理：
1. 创建RemoteApp A，关联 `.txt`
2. 创建RemoteApp B，尝试关联 `.txt`
3. 验证程序的冲突处理逻辑

### 测试图标变化
1. 创建关联并设置图标A
2. 编辑关联，更改为图标B
3. 验证文件图标是否更新

---

**测试完成！** 🎉

如有任何问题，请查看程序的调试输出或联系开发团队。
